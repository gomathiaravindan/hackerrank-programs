#include<stdio.h>
#include<string.h>
int main()
{
    int i,count=1,len;
    char s[100000];
    scanf("%s",s);
    len=strlen(s);
    for(i=0;i<len;i++)
    {
        if(s[i]>='A' && s[i]<='Z')
        {
            count++;
        }
    }
    printf("%d",count);
}
